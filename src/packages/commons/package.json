{
  "name": "@pontoon-tools/commons",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "webextension-polyfill": "^0.4.0"
  },
  "devDependencies": {
    "eslint": "^6.6.0"
  },
  "eslintConfig": {
    "parserOptions": {
      "sourceType": "module"
    }
  },
  "scripts": {
    "lint": "eslint . --ignore-pattern /build/",
    "test": "echo 'No tests!'",
    "prebuild": "yarn clean",
    "build": "mkdir -p ./build && cp ./src/* ./build/",
    "clean": "rm -rf ./build"
  }
}

{
  "name": "@pontoon-addon/options",
  "version": "0.0.0",
  "private": true,
  "homepage": ".",
  "scripts": {
    "build": "run-s clean webpack:run eslint:run",
    "test": "echo 'No tests!'",
    "clean": "rimraf dist",
    "webpack:run": "webpack ./src/index.js --output-path ./dist --config ./webpack.config.js",
    "eslint:run": "eslint --ignore-pattern /dist/ ./src/",
    "eslint:fix": "eslint --ignore-pattern /dist/ --fix ./src/"
  },
  "dependencies": {
    "@pontoon-addon/commons": "*",
    "webextension-polyfill": "^0.7.0"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2",
    "webpack": "^4.44.2",
    "webpack-cli": "^3.3.12"
  }
}

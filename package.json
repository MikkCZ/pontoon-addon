{
  "name": "pontoon-tools",
  "version": "3.4.5",
  "private": true,
  "description": "Tools for Pontoon and its integration into the browser.",
  "author": "Michal Stanke <mstanke@mozilla.cz>",
  "license": "MPL-2.0",
  "homepage": "https://github.com/MikkCZ/pontoon-tools#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MikkCZ/pontoon-tools.git"
  },
  "bugs": {
    "url": "https://github.com/MikkCZ/pontoon-tools/issues"
  },
  "workspaces": [
    "src/packages/*"
  ],
  "dependencies": {
    "moment": "^2.24.0",
    "moment-duration-format": "^2.3.2",
    "webextension-polyfill": "^0.4.0"
  },
  "devDependencies": {
    "eslint": "^6.2.2",
    "jsdoc": "^3.6.3",
    "lerna": "^3.16.4",
    "npm-run-all": "^4.1.5",
    "web-ext": "^3.1.1"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "eslint": "eslint ./src",
    "web-ext-lint": "web-ext lint -s ./src",
    "lint": "npm-run-all eslint web-ext-lint",
    "pretest": "yarn lint",
    "test": "lerna exec --parallel -- CI=true yarn test",
    "prebuild": "yarn test",
    "build-chrome": "mkdir -p ./build/chrome-src && cp -Lr ./src/* ./build/chrome-src && mv ./build/chrome-src/manifest-chrome.json ./build/chrome-src/manifest.json && web-ext build -s ./build/chrome-src -a ./build/web-ext/chrome",
    "build-firefox": "web-ext build -s ./src -a ./build/web-ext/mozilla",
    "build": "npm-run-all build-firefox build-chrome",
    "doc": "jsdoc ./src ./README.md -r -d ./build/docs",
    "clean": "rm -rf ./build"
  }
}
